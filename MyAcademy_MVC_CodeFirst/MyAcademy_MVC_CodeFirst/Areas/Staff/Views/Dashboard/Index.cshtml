@using MyAcademy_MVC_CodeFirst.Data.Entities
@model AppUser
@{
    ViewBag.Title = "Genel Bakış";
    Layout = "~/Areas/Staff/Views/Shared/_StaffLayout.cshtml";
    var lastSales = ViewBag.LastSales as List<Sale>;
}

<div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-6">

    <div class="bg-gradient-to-r from-blue-600 to-blue-800 rounded-xl p-6 text-white shadow-lg relative overflow-hidden">
        <div class="relative z-10">
            <h2 class="text-2xl font-bold mb-2">Merhaba, @Model.FullName 👋</h2>
            <p class="text-blue-100 text-sm">Bugün harika işler çıkarmaya hazırsın!</p>
            <a href="/Staff/Sales/NewSale" class="inline-block mt-4 bg-white text-blue-700 px-4 py-2 rounded-lg text-sm font-bold hover:bg-blue-50 transition-colors">
                + Yeni Satış Gir
            </a>
        </div>
        <span class="material-symbols-outlined absolute -right-4 -bottom-4 text-[150px] text-white/10">waving_hand</span>
    </div>

    <div class="bg-white p-6 rounded-xl shadow-sm border border-slate-100 flex items-center justify-between">
        <div>
            <p class="text-sm text-slate-500 font-medium uppercase">Toplam İşlem</p>
            <h3 class="text-3xl font-bold text-slate-800 mt-1">@ViewBag.TotalSales</h3>
        </div>
        <div class="p-3 bg-green-100 text-green-600 rounded-full">
            <span class="material-symbols-outlined text-3xl">shopping_cart</span>
        </div>
    </div>

    <div class="bg-white p-6 rounded-xl shadow-sm border border-slate-100 flex items-center justify-between">
        <div>
            <p class="text-sm text-slate-500 font-medium uppercase">Toplam Ciro</p>
            <h3 class="text-3xl font-bold text-slate-800 mt-1">₺@(((decimal)ViewBag.TotalRevenue).ToString("N0"))</h3>
        </div>
        <div class="p-3 bg-purple-100 text-purple-600 rounded-full">
            <span class="material-symbols-outlined text-3xl">payments</span>
        </div>
    </div>
</div>

<div class="bg-white rounded-xl shadow-sm border border-slate-100 overflow-hidden">
    <div class="p-6 border-b border-slate-100 flex justify-between items-center">
        <h3 class="font-bold text-slate-800 text-lg">Son 5 Satış İşlemi</h3>
        <a href="/Staff/Sales/Index" class="text-sm text-blue-600 hover:text-blue-800 font-medium">Tümünü Gör →</a>
    </div>
    <div class="overflow-x-auto">
        <table class="w-full text-sm text-left">
            <thead class="bg-slate-50 text-slate-500 uppercase text-xs">
                <tr>
                    <th class="px-6 py-3">Müşteri</th>
                    <th class="px-6 py-3">Poliçe</th>
                    <th class="px-6 py-3">Tarih</th>
                    <th class="px-6 py-3 text-right">Tutar</th>
                </tr>
            </thead>
            <tbody>
                @if (lastSales != null)
                {
                    foreach (var item in lastSales)
                    {
                        <tr class="border-b border-slate-50 hover:bg-slate-50/50 transition-colors">
                            <td class="px-6 py-4 font-medium text-slate-900">
                                @(item.Customer != null ? item.Customer.FirstName + " " + item.Customer.LastName : "Silinmiş Müşteri")
                            </td>

                            <td class="px-6 py-4">
                                @if (item.InsurancePolicy != null)
                                {
                                    <span class="bg-blue-50 text-blue-700 px-2 py-1 rounded text-xs font-semibold border border-blue-100">
                                        @item.InsurancePolicy.PolicyName
                                    </span>
                                }
                                else
                                {
                                    <span class="bg-red-50 text-red-700 px-2 py-1 rounded text-xs font-semibold border border-red-100">
                                        Silinmiş Poliçe
                                    </span>
                                }
                            </td>

                            <td class="px-6 py-4 text-slate-500">
                                @item.SaleDate.ToString("dd MMM yyyy, HH:mm")
                            </td>

                            <td class="px-6 py-4 text-right font-bold text-slate-700">
                                ₺@item.Amount.ToString("N2")
                            </td>
                        </tr>
                    }
                }
            </tbody>
        </table>
    </div>
</div>
